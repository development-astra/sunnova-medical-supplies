version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: sunnova_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: sunnova
      POSTGRES_PASSWORD: sunnova_pass
      POSTGRES_DB: sunnova_db
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U sunnova -d sunnova_db']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: sunnova_redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  # Optional: local S3-compatible storage for development
  # minio:
  #   image: minio/minio:latest
  #   container_name: sunnova_minio
  #   restart: unless-stopped
  #   command: server /data --console-address ":9001"
  #   environment:
  #     MINIO_ROOT_USER: minioadmin
  #     MINIO_ROOT_PASSWORD: minioadmin
  #   ports:
  #     - '9000:9000'
  #     - '9001:9001'
  #   volumes:
  #     - minio_data:/data

volumes:
  postgres_data:
  redis_data:
  # minio_data:
